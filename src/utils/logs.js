// Classe responsável por registrar mensagens no console
export class MessageLogger {
    // Método estático para registrar uma mensagem no console
    static logMessage(message) {
        console.log(message);
    }
}
